var React = require('react');

var Header =
  React.createClass({
    render: function() {
      return (
        <h1>
          My Flux App
        </h1>
        );
    }
  });

module.exports = Header;
